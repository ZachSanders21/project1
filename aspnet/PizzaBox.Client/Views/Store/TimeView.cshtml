@model PizzaBox.Client.Models.StoreViewModel
@{
    ViewBag.Title = "Customer - Home";
    Layout = "_CustomerLayout";
}
<form asp-action="TimeSelect" asp-controller="store" asp-antiforgery="true" method="POST">
    <p>
        Select View
    </p>
    @foreach (var item in Model.TimeSet)
    {
        <input name="timeselect" type="radio" asp-for="@Model.TimeSet" value="@item" required>
            <label for="@item">@item</label>
    }
    <button type="submit">Submit Selection</button>
    <form asp-action="home" asp-controller="Home" asp-antiforgery="true" method="GET">
    <button type="submit">Home</button>
    </form>
</form>